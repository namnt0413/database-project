
TRANG CHỦ BÁN HÀNG
- Sửa lại form đăng kí để có thể đăng kí được cả sđt và địa chỉ



TRANG ADMIN :
-   Lấy ra tên người dùng từ database và hiển thị ra ở thanh navbar trên cùng  (phải sửa lại code phần đầu trên tất cả các file (phần check xem có tồn tại user đang đăng nhập không) cho giống với file index.php )
-   fix lại thanh tìm kiếm .au-input--xl { min-width: 735px; } - trước max-width là 935px sẽ bị chèn vào chữ

- Đổi tên các hàm liên quan đến user về dạng user_ ... cho dễ nhìn:v
- Ghép các hàm user.php, user_create, user_delete, user_edit với giao diện 
- Fix lỗi và hoàn thiện các hàm trên