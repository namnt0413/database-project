"use-strict";

const input_area = document.querySelector(input_area);

document.querySelector(".button").addEventListener("click", function (e) {
  input_area.array.forEach((element) => {
    console.log(element);
  });

  // if(phone.value)
});
